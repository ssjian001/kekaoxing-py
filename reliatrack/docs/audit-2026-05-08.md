# ReliaTrack 代码审计报告 — 2026-05-08

## 审计概要

| 指标 | 数值 |
|------|------|
| 总文件 | 84 Python, 19,021 行 |
| Schema | v14 |
| 测试 | 106 (36 安全回归 + 70 现有) |

---

## 修复记录

### Commit 82e7e30 — P0 (9 项, +67 -21)
1. `base.py`: `_safe_kwargs()` 方法过滤 kwargs 白名单; `insert()`/`update()` 应用
2. `sample_repo.py`: `_TXN_SAFE_COLS` 白名单 in `add_transaction()`
3. `issue_repo.py`: `_FA_SAFE_COLS` 白名单 in `add_fa_record()`, `_ATTACH_SAFE_COLS` in `add_attachment()`
4. `export_service.py:2201`: `_set_cell_shading()` — `re.fullmatch(r"[0-9A-Fa-f]{6}", hex_color)` 校验
5. `plan_handlers.py:304`: `data.pop("id")` → `data.get("id")` + dict comprehension 排除 id
6. `issue_handlers.py:179`: 同上模式, `"issue_id"` → `fa_data`
7. `project_handlers.py:54`: 同上 `data.pop("id")` 修复
8. `test_plan_view.py:914`: `setup_task_callbacks()` — `disconnect()` before `connect()` for 3 buttons

### Commit 39a1323 — P1 关键 (15 项, +124 -25)
1. `export_service.py`: `_validate_output_path()` + 6 处导出 save 包裹 try/except
2. `issue_repo.py`: `_remove_disk_file()` — `_ALLOWED_ATTACH_DIRS` 路径前缀校验
3. `base.py`: `delete()`/`list_all()`/`count()` — try/except + logger
4. `refresh_handlers.py`: 移除 `._repo` 直访, 用 `ctrl.sample_service.count_by_status()`
5. `sample_service.py`: 新增 `count_by_status()` 委托方法
6. `export_handlers.py`: 用 `ctrl.export_service` 而非自建; WaitCursor
7. 6 个 handler 文件: 加 `import logging` + `logger`
8. `task_dialog.py:633`: `json.loads()` 包裹 try/except JSONDecodeError

### Commit 6eafa3e — P1 补充 (+42 -57)
1. `_TXN_COLS`/`_FA_COLS`/`_ATTACH_COLS`/`_CAPA_SELECT_COLS` 改 tuple 消除双重维护
2. `theme.py`: `filter_combo_qss()` DRY 工厂函数
3. `main.py`: 两个 ComboBox 用 `filter_combo_qss()` 替代重复 20 行 QSS

### Commit 2c9d37c — 测试 (+523)
1. 36 个安全回归测试覆盖所有 P0/P1 修复点
2. Schema 迁移链修复: v11 `capa_records` rebuild 缺 `assignee_name`
3. `conftest.py` `collect_ignore` 排除脚本式测试

### Commit 71fbdfb — P2 运行时安全 (+57 -23)
1. `export_handlers:59`: `except: pass` → `logger.exception()`
2. `plan_handlers`: `_on_task_add`/`_on_task_delete` 包裹 try-except + QMessageBox
3. `issue_repo:242`: `update_capa_record` safe 空字典提前 return
4. `undo_manager:175`: `DeleteEntityCommand.undo` ID 冲突检测 + fallback auto-ID
5. `sample_handlers:113`: 出库操作 `with repo.transaction()` 原子性
6. `scheduler_service:200`: `copy.deepcopy(tasks)` 防污染原始对象
7. `sample_handlers:174`: 批量导入 toast 显示 skip 计数

### 已验证无误报 (5 项)
1. `_open_dep_selector` — `QDialog(self)` 已继承父组件样式
2. `TestResultDialog(QWidget)` — 设计为嵌入式容器, 外部已有 QDialog
3. `accept()` 中 `get_data()` 只调用一次, 报告误判
4. `sectionResized` lambda — debounce timer 已更新引用
5. `currentTextChanged` — 连两个不同 slot, 正确 Qt 模式

---

## 待修复项目

### P1 待修 (1 项)
| ID | 问题 | 文件 | 说明 |
|----|------|------|------|
| rem-8 | `_refresh_dashboard()` 150行 KPI 逻辑 | `refresh_handlers.py:190-298` | 需新建 `DashboardService`, 本质是重构非 bug |

### P2 待修 (按优先级排序)

#### 高优先级 (运行时风险)
| ID | 问题 | 文件:行 | 说明 |
|----|------|---------|------|
| P2-IO | 导出综合报告在 WaitCursor 下执行大量 IO | `export_handlers.py:78-250` | 需 QThread 重构, 大文件/网络路径时 UI 长时间无响应 |
| P2-TXN | `bulk_update_start_day` 手动事务嵌套 | `test_task_repo.py:104-117` | 用 `transaction()` 上下文管理器替代 begin/commit/rollback |
| P2-FONT | CJK 字体检测 `except Exception: continue` | `export_service.py:56-57` | 全部候选失败时 CJK_FONT 可能不存在, PDF 导出报错不明确 |

#### 中优先级 (数据一致性)
| ID | 问题 | 文件:行 | 说明 |
|----|------|---------|------|
| P2-REDO | `UndoManager.execute()` 异常清空 redo 栈 | `undo_manager.py:198-204` | `command.do()` 失败时 redo 已清空但 undo 未增长 |
| P2-ATOMIC | 入库操作 `add_transaction` + `update_status` 非原子 | `sample_handlers.py:80` | 入库同出库问题, 需 transaction 包裹 |
| P2-PROGRESS | `update_task_progress` status 推导与 DB 竞态 | `test_plan_service.py:73-75` | progress=100 但 DB status 已 completed 时触发不必要 update |

#### 低优先级 (健壮性)
| ID | 问题 | 文件:行 | 说明 |
|----|------|---------|------|
| P2-BACKUP | `_startup_backup` 失败后继续无磁盘空间预警 | `app_controller.py:145-146` | 备份失败仅 log, 后续 DB 操作也可能失败 |
| P2-ORPHAN | `delete_by_plan` 附件文件删除失败仅 warning | `test_task_repo.py:134-141` | DB CASCADE 删除但磁盘文件成孤儿 |
| P2-IMPORT | import_service 失败行不展示给用户 | `import_service.py:71/120` | handler 未检查 `result.errors` 展示详情 |

---

## 测试覆盖

### 现有测试文件
| 文件 | 测试数 | 说明 |
|------|--------|------|
| `test_services.py` | 20 | Service 层 CRUD + Undo |
| `test_new_features.py` | 16 | 新增功能 (count/status/delete/validation) |
| `test_column_order.py` | 11 | 列序映射回归 |
| `test_boundary.py` | 7 | Dialog 构造 + 边界场景 |
| `test_security_regression.py` | 36 | P0/P1 修复回归 |
| `test_e2e_full.py` | (skip) | 脚本式 E2E, `python tests/test_e2e_full.py` |
| `test_performance.py` | (skip) | 性能基准, `python tests/test_performance.py` |

### 待补测试
- P2 运行时安全修复的回归测试 (undo ID 冲突, 出库原子性, capa 空字典)
- `import_service` 的错误路径测试
- scheduler_service 的对象隔离测试

---

## 关键文件速查

| 用途 | 文件 |
|------|------|
| 主入口 | `main.py` (380行) |
| Schema | `src/db/schema.py` — v14, `init_schema()`, `_migrate_v1~v14` |
| Repo 基类 | `src/db/repositories/base.py` — `_safe_kwargs()`, `transaction()` |
| 列宽持久化 | `src/styles/column_persistence.py` — debounce 300ms |
| 主题 | `src/styles/theme.py` — Catppuccin Latte, `filter_combo_qss()` |
| 导出 | `src/services/export_service.py` — Excel/PDF/Word, `_validate_output_path()` |
| 排程 | `src/services/scheduler.py` + `scheduler_service.py` — 3 阶段算法 |
| 撤销 | `src/services/undo_manager.py` — Command 模式, max_history=50 |
